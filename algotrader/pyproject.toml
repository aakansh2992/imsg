[project]
name = "algotrader"
version = "0.1.0"
description = "Confluence-based intraday XAUUSD algo trader (stdlib only)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["algotrader*"]
